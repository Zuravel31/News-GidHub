# Используем официальный образ Memcached из Docker Hub.  Это базовая операционная система и сам Memcached.
FROM memcached:latest

# Опционально: Установка дополнительных утилит (например, telnet для тестирования).
# Если вы используете telnet, убедитесь, что это не нарушает безопасность.
# RUN apt-get update && apt-get install -y telnet

# Опционально:  Копируем пользовательскую конфигурацию Memcached (если необходимо).
# Создайте файл memcached.conf (см. ниже) и раскомментируйте следующие строки, если хотите использовать его.
# COPY memcached.conf /usr/local/etc/memcached.conf

# Открываем порт Memcached (11211 - стандартный порт) для доступа извне (например, из вашего приложения).
EXPOSE 11211

# Устанавливаем команду по умолчанию для запуска Memcached.
# Здесь мы можем указать параметры запуска (например, ограничить объем памяти)
# -m 64:  Выделяем 64MB памяти для кэша.  Отрегулируйте это значение в зависимости от ваших потребностей.
CMD ["memcached", "-m", "64", "-p", "11211"]